# Loaded on top of application.properties when the "test" profile is active.
# Only the database changes: tests get their own wordle_test database, so they
# never read or write the development data in wordle. Flyway builds its schema
# from the same migrations on the first run.
spring.datasource.url=jdbc:postgresql://localhost:5432/wordle_test
